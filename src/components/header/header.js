import React, {Component} from 'react';
import './header'

const Header = () => (
    <nav>
        <div className="row">
            <div className="logo">
                <h1>PayPal UI</h1>
            </div>
        </div>
    </nav>
)

export default Header;